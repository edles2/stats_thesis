# Example Input Placeholder

The real workbook is excluded for privacy.

Use `analysis/private_data/thesis_source_data.xlsx` locally if you have access.
For public smoke checks, run `python smoke.py` from `analysis/`.
